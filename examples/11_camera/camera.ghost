class Camera {
  function constructor() {
    this.x = 0
    this.y = 0
  }

  function follow(object) {
    this.x = -object.x + (window.width / 8) - 8
    this.y = -object.y + (window.height / 8) - 8
  }

  function draw() {
    canvas.translate(this.x, this.y)
  }
}