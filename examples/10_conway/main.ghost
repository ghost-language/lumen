import World from './world'

world = World.new()

function load() {
  window.title("Conway's Game of Life")
  window.resize(800, 600)
  
  world.load()
}

function update() {
  world.update()
}

function draw() {
  graphics.print('FPS: ' + window.fps.toString(), 10, 10)
  
  world.draw()
}